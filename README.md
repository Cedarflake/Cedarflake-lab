# Focus Orb

A pnpm workspace containing a reusable React WebGL focus orb package and a Vite demo app.

## Workspace

- `packages/focus-orb`: reusable React component package
- `apps/demo`: demo site using the package through `workspace:*`

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm check
```
